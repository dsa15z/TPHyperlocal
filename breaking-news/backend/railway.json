{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "npx prisma db execute --schema prisma/schema.prisma --file prisma/sql/add-platform-values.sql || echo 'platform-enum hotfix skipped (non-fatal)'; node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
